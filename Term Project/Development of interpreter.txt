Presently the interpreter does only addition and subtraction of numbers by tokenizing and parsing the input given
We're trying to extend this by further adding the production rules of our language which is Google Go.